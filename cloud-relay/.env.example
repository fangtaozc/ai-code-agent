# cloud-relay 配置文件
# 本地使用时保持默认即可，不需要修改任何内容。

# 服务端口，默认 3000。如果 3000 被占用，改成其他端口比如 3001。
PORT=3000

# 运行环境，保持 production 即可。
NODE_ENV=production

# 登录密码（可选）
# 留空 = 不需要登录，直接访问（本地使用推荐留空）。
# 填写后，访问网页会要求输入这个密码。适用于把服务部署在公网服务器上时使用。
# 示例：ACCESS_TOKEN=my-secret-password
ACCESS_TOKEN=
